# 👨‍⚕️ WordPress Doctors Directory (Test Assignment)

Репозиторий содержит решение тестового задания: реализация каталога врачей на WordPress с использованием кастомных типов записей (CPT), таксономий и фильтрации.

Проект полностью контейнеризирован (Docker) и включает скрипты для автоматического развертывания и наполнения демо-данными.

---

## 📋 Содержание

- [Функциональность](#-функциональность)
- [Технологический стек](#-технологический-стек)
- [Архитектурные решения](#-архитектурные-решения)
- [Установка и запуск](#-установка-и-запуск)
- [Структура проекта](#-структура-проекта)
- [Проверка работы (URL)](#-проверка-работы-url)
- [Доступы](#-доступы)

---

## 🚀 Функциональность

1.  **CPT "Доктор" (`doctors`):**
    *   Поддержка заголовка, редактора, миниатюры, отрывка.
    *   Нативные мета-поля: **Стаж**, **Цена приема**, **Рейтинг**.
    *   Безопасное сохранение и вывод данных (sanitization/escaping).

2.  **Таксономии:**
    *   **Специализация** (`specialization`) — иерархическая.
    *   **Город** (`city`) — иерархическая (для возможности группировки Регион -> Город).

3.  **Архив и Фильтрация:**
    *   Фильтрация по нескольким параметрам одновременно (GET-запросы).
    *   Сортировка (по рейтингу, цене, стажу).
    *   Корректная пагинация при активных фильтрах.

4.  **Автоматизация:**
    *   Python-скрипты разворачивают окружение, устанавливают WP, настраивают ЧПУ и генерируют контент.

---

## 🛠 Технологический стек

*   **WordPress:** Latest
*   **PHP:** 7.4+ (совместим с 8.x)
*   **Docker / Docker Compose:** Изоляция среды.
*   **MySQL:** 5.7
*   **WP-CLI:** Для автоматизации настройки.
*   **Python 3:** Оркестрация установки.

**Важно:** В проекте **не используются** плагины-конструкторы (Elementor, Divi) или плагины для полей (ACF, Carbon Fields). Вся логика реализована на нативном API WordPress.

---

## 🧠 Архитектурные решения

### 1. Разделение логики и представления
*   **Плагин `doctors-cpt`:** Отвечает за регистрацию CPT, таксономий, создание метабоксов и модификацию SQL-запросов. Это гарантирует, что при смене темы данные не пропадут.
*   **Тема `developers-theme`:** Отвечает исключительно за HTML-вывод (шаблоны `archive-doctors.php`, `single-doctors.php`).

### 2. Использование `pre_get_posts`
Для фильтрации на странице архива используется хук `pre_get_posts`, а не создание нового `WP_Query` в шаблоне.
*   **Причина:** Это наиболее производительный метод ("The WordPress Way"). Он модифицирует основной запрос до обращения к БД, что обеспечивает корректную работу пагинации и не создает лишней нагрузки.

### 3. Нативные Meta Boxes
Вместо ACF использован `add_meta_box` и `update_post_meta`.
*   **Причина:** Демонстрация умения работать с ядром WP, отсутствие зависимостей, максимальная скорость работы админки.

### 4. Автоматическая генерация .htaccess
Установщик принудительно создает и настраивает `.htaccess` и сбрасывает permalinks, чтобы исключить типичные ошибки "404 Not Found" при развертывании CPT на чистом сервере.

---

## 📥 Установка и запуск

Для запуска требуются установленные **Docker Desktop** и **Python 3**.

### Шаг 1. Клонирование

```bash
git clone https://github.com/Joeso/testwork.git
cd testwork
Шаг 2. Запуск контейнеров
Выполните скрипт, который поднимет MySQL и WordPress:
python start.py
(Windows: если python не найден, попробуйте py start.py)

⏳ Важно: После запуска контейнеров подождите 1-2 минуты, пока база данных MySQL инициализируется.

Шаг 3. Установка и наполнение
Запустите скрипт установки. Он настроит WP, активирует плагины и создаст 12 демо-докторов:
python install.py
Если скрипт сообщит, что "MySQL не готов", подождите еще немного и запустите его снова.
📂 Структура проекта
/
├── start.py                 # Скрипт поднятия Docker
├── install.py               # Скрипт установки WP и данных
├── docker-compose.yml       # Конфигурация контейнеров
├── wp-content/
│   ├── plugins/
│   │   └── doctors-cpt/     # [ПЛАГИН] Логика
│   │       ├── doctors-cpt.php
│   │       ├── includes/
│   │       │   ├── class-post-type.php      # Регистрация CPT
│   │       │   ├── class-taxonomies.php     # Регистрация таксономий
│   │       │   ├── class-meta-boxes.php     # Мета-поля
│   │       │   └── class-archive-filter.php # Логика фильтрации
│   │       └── assets/
│   │           └── css/
│   └── themes/
│       └── developers-theme/# [ТЕМА] Шаблоны
│           ├── archive-doctors.php  # Вывод списка врачей
│           ├── single-doctors.php   # Страница врача
│           └── template-parts/
│               └── doctor-card.php  # Компонент карточки
✅ Проверка работы (URL)
После успешной установки сайт доступен по адресу http://localhost:8080.

Сценарий	URL
Главный архив	http://localhost:8080/doctors/
Фильтр: Кардиологи	http://localhost:8080/doctors/?specialization=cardiologist
Фильтр: Москва + Сортировка по цене	http://localhost:8080/doctors/?city=moscow&sort=price_asc
Комбинированный фильтр	http://localhost:8080/doctors/?specialization=surgeon&city=spb&sort=rating_desc
🔑 Доступы
Админка: http://localhost:8080/wp-admin/
Логин: admin
Пароль: admin123
Полезные команды
Если нужно удалить проект и данные (полная очистка):
docker-compose down -v